name: device_location
description: Location service implementation using geolocator + permission_handler.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^13.0.2
  permission_handler: ^11.3.1
  agri_core:
    path: ../agri_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
