name: agri_core
description: Shared domain entities, use cases, repository contracts, and infrastructure abstractions for AgriGate.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.5
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  dio: ^5.7.0
  supabase: ^2.10.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.13
